abstract class CurrencyLocalDataSource {
  // Local data source for currency conversion (no history functionality)
}

class CurrencyLocalDataSourceImpl implements CurrencyLocalDataSource {
  // Implementation for local data source (no history functionality)
} 